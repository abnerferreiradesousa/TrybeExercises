import { render, screen } from '@testing-library/react';
import App from './App';

describe('Testando inputs', () => {

  test('renders learn react link', () => {
    render(<App />);
    //se o input é renderizador
    //se o input tem como valor ''
    //se o input tem como valor Estudante...''
    //input email mesma coisa
  });

})
